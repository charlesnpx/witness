// Package contracts implements Witness's versioned review contract family.
//
// Relay survival validates only the filed witness. It never validates the
// finding remedy, proposed tests, or provider command execution.
package contracts
